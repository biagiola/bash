# create a bash script
touch init.sh

# give permissions to execute that file
sudo chmod +x init.sh

# inside the file we must to specify the dialect of
# our bash file, put it this to the first line: #! /bin/bash

# run the file
sudo bash init.sh
